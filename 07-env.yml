apiVersion: v1
kind: pod
metadata:
  name: env-demo
spec: 
  containers:
    - name: nginx
      image: nginx
      env: 
      - name: projecct
        value: roboshop
      - name: course
        value: kubernetes